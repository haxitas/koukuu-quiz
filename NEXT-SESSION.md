# 次のセッションへの引き継ぎ(2026-07-22時点)

このファイルは別セッションでの再開用。全体の憲法・データモデルは `CLAUDE.md`、経緯・学んだ教訓は `MUSENN-PHASE_NOTES.md` を参照(このファイルはそれらを読んだ前提で「次に何をするか」だけに絞った短い指示書)。

## 今やること: 無線工学を令和期12期に追加

法規は令和期13期(R2-02〜R8-02)が完成済み(全問公式正答と完全一致確認済み)。次は**無線工学**を、まだ工学が無い12期に追加する。

対象期(工学が未投入。**R8-02のみ既に工学14問あり、対象外**):
`R7-08, R7-02, R6-08, R6-02, R5-08, R5-02, R4-08, R4-02, R3-08, R3-02, R2-08, R2-02`

## 今はやらないこと(指示があるまで着手しない)

- 英語・英会話の追加(後回し)
- GitHub Pages公開(後回し)
- 平成期以前(2002〜2019)への展開

## 工学が法規と違う点

- 各期の `data/{code}.json` は**法規が既に入っている既存ファイル**。工学の問題は既存の `questions` 配列に**追記**する形になる。法規の既存要素は絶対に書き換えない。
- 図が必要な問題がある(`data/R8-02.json` の無線工学部分を見本にする。図あり・図なし両方のパターンを含む)。`scripts/crop_figures.py` で切り出し、`figures/{code}-kougaku-{no}.png` に保存。範囲は少し広めに切ってReadツールで目視確認→隣接設問がはみ出していれば1〜2回微調整、という確立済みの手順(詳細はMUSENN-PHASE_NOTES.mdのPhase1の記録を参照)。
- 正答は必ず `{code}-kougaku-kaitou.pdf` を画像化して読む(テキスト抽出は使わない。条文・表の順序が乱れることが判明済み)。

## モデルと利用制限への対応(最重要)

工学は**Opus**を使う想定。画像を多く読む(問題文+図+正答表)ため、**5時間の利用制限にすぐ当たる可能性が高い**。制限に当たっても「また一から」にならないよう、必ず以下を守る:

1. **1問(または図1枚)終わるごとに `data/{code}.json` を書き直して保存する**。14問終わってからまとめて書かない。`validate_data.py` は問題の総数を強制していない(存在する問題だけを検査する)ので、部分保存(例: 8問しか入っていない状態)でもエラーにならない。
2. **作業前に必ず既存ファイルを読む**。`data/{code}.json` を開き、`subject:"無線工学"` の問題が何問・どのno番号まで入っているか確認してから続きから始める。ゼロからやり直さない。
3. 図も同様に、`figures/{code}-kougaku-{no}.png` が既に存在するか先に確認し、あれば再生成しない。
4. エージェントに割り当てる期は1体あたり1〜2期程度(法規より重いので欲張らない)。
5. 制限で止まった場合、**同じ期の続きから**(書き込み済みのno番号の次から)再開する指示を出す。新しいセッション/エージェントには「`data/{code}.json` を先に読んで無線工学が何問入っているか確認し、続きの問題番号から始めて」と必ず伝える。

## エージェントへの指示文(法規で使ったプロンプトを流用・改変)

法規変換で使った指示文(このリポジトリの git log 上のコミットメッセージ・このセッションの会話履歴に実物が残っている)をベースに、以下を変更・追加する:

- 見本を `data/R8-02.json` の無線工学部分に変更(図あり・図なし両パターン)。
- 「`data/{コード}.json` は既に法規20問が入っている。読み込んで既存の `questions` 配列に無線工学の問題を追加する。法規部分は一切変更しない」を明記。
- 「1〜2問ごとに書き込んで保存する」を追加(法規のときの「1期終わるごとに保存」よりさらに細かく)。
- 図の切り出し手順(`crop_figures.py` で生成→Readツールで確認→はみ出しがあれば座標を調整して再生成)を明記。
- 「作業開始前に `data/{コード}.json` を読み、無線工学が何問入っているか確認し、続きの問題番号から始める」ことをエージェントへの指示の最初に明記(再開されたエージェントが重複作業しないため)。

## 完了確認の手順(法規のときと同じ)

1. 正答PDF(kaitou)を画像化して自分で目視突き合わせ。
2. `python scripts/validate_data.py` でエラー0を確認。
3. `python scripts/build_index.py` でindex.json再生成(**全期そろってから最後に1回だけ実行**。個別エージェントには実行させない)。
4. アプリで無線工学を全問正答させて100%になるか確認。ブラウザのキャッシュ事故対策として `fetch(url,{cache:'reload'})` で対象ファイルを温めてから `location.reload()` する(既存タブの単純reloadだと古いモジュール/データを掴むことがある。詳細はMUSENN-PHASE_NOTES.md参照)。
5. `tests/run-tests.html` が緑のままか確認してからコミット。

## 参照

- 全体のデータモデル・憲法: `CLAUDE.md`
- これまでの経緯・学んだ教訓: `MUSENN-PHASE_NOTES.md`(特に「1期終わるごとに都度保存」で2回目のエージェント再開が全滅を免れた経緯)
- ローカル起動: `start-local.bat` をダブルクリック、またはプロジェクトルートで `python -m http.server 8137`
