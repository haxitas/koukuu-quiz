# MUSENN PHASE NOTES

## 全期間展開(法規) — 2026-07-22、令和期13期分が完了

### 状況
- 琥珀の指示:「工学以外の分野を全期間展開して」→ ペースは「令和分だけで」、科目優先度は「法規を優先」。
- 対象: 令和期13期(R2-02〜R8-02)の**法規**。**全13期・260問が完成**(R8-02は元々4科目揃っていたので実質12期分を今回追加)。

### 経緯(1回目・利用上限で中断)
- R7-08を自分で仕上げてテンプレート(スキーマ・転記手順)を確立。
- 残り11期を5体の並列エージェントに2〜3期ずつ割り当てて起動 → **全5体がアカウントの月間利用上限(API spend limit)に到達して停止**。書き込みまで到達したのはR3-02・R5-02の2期のみ(検証してコミット済み)。
- 残り9期は着手前または途中で停止しファイルが存在せず、作業内容は失われた。

### 経緯(2回目・再開して完了)
- 琥珀が時間経過で利用上限の解放を確認 →「5体のエージェントをつかって再開して」の指示で再起動。
- 残り9期(R7-02, R6-08, R6-02, R5-08, R4-08, R4-02, R3-08, R2-08, R2-02)を同じ5体構成で再割り当て。**今回は指示文に「1期終わるごとに都度保存」を追加**し、途中失敗しても成果が残るようにした(結果的に全5体が完走)。
- 全9期を(a)公式正答PDFを自分で画像化し目視で全問突き合わせ、(b)アプリで全問正答→20/20・100%、の二重検証。**全問・全期で公式正答と完全一致**(転記ミス0件)。
- `build_index.py`で index.json を13期・293問に再生成、`validate_data.py`通過、ユニットテスト31/31緑を確認してコミット。

### 確立できたこと(次に活きる知見)
- 法規は図が無いため`figure`は常にnull、工学より軽い(画像読解のみでクロップ作業が不要)。
- 転記テンプレート(完成済みJSON1期分を正解見本として渡す方式)は精度面で安定して機能した(合計12期・240問超すべて公式正答と完全一致)。
- **並列エージェントには「1期終わるごとに都度ファイル保存」を必ず指示する**。複数期を1体に割り当てる場合、最後にまとめて保存する設計だと、途中で(利用上限・その他の理由で)止まったときに全部失われる。今回この一手間で2回目は全滅を回避できた。
- 1エージェントあたり2〜3期の割り当てはボリューム的に無理はない。

### 次の初手
1. 法規が令和期13期で完了したので、次は琥珀の優先順位どおり**英語・英会話**に進む(工学は指示があるまで対象外)。
2. 平成期(2002〜2019、令和より前)への展開は別途指示があれば着手。今は令和期のみの約束のためここで止める。

## Phase2(復習モード — 2026-07-22、一部前倒しで着手)

### 完了したこと
- **誤答バンク(復習モード)を実装**。琥珀の要望:「前回間違えた問題を科目ごとに載せ、正解したら消える。また間違えたら残り続ける。1回の出題数は最大10問」に対応。
- **設計**: 可変ストアを新設せず、既存の append-only `attempts` から都度導出する方式にした(憲法1と自然に両立)。
  - `Attempt` に `presented`(その回に出題した問題idの一覧)を追加。`makeAttempt(key, date, examResult, presented)`。
  - `quiz-core.currentMistakes(store, key)`: あるidの**直近の出題(presentedに含まれた回)時に誤答だったか**で判定。正解すれば次の集計から自然に消え、誤答すれば残る。一度も再提示されていない誤答は未解決のまま残る(= 復習で扱わなかった問題は勝手に消えない)。
  - `quiz-core.pickReviewQuestions(questions, mistakeIds, max=10)`: 出題順を保ったまま誤答idを最大10件に絞る。
- **UI**: 選択画面の各科目に、誤答が1件以上あれば「復習（誤答N問）」ボタンを表示(10件超は「誤答N問中10問」と明示)。復習回は出題画面に「（復習）」タグ、採点結果に「この分野の誤答バンク: N問」を表示(0件なら「すべて解消」)。
- 通常回の「前回まちがえた」個別バッジ(Phase1で実装済み)は、この誤答バンクを参照する形に統一(旧・直近1回だけを見る実装から置き換え)。復習回では全問がバンク由来で自明なため個別バッジは出さない。
- **TDD**: currentMistakes/pickReviewQuestions/makeAttempt(presented込み)のテストを先に書き赤を確認(モジュールimportエラーで停止することを確認)→実装→緑(28/28)。意図的バグ(判定を反転)で4件が赤に転じることも確認→復帰。
- **実機検証**(複数シナリオ、いずれもブラウザの実イベントで確認):
  - 無線工学14問中3問(A-1〜A-3)を誤答→選択画面に「復習（誤答3問）」→復習でA-1,A-2正答・A-3誤答のまま→バンクは1問(A-3)に減少、選択画面のラベルも自動更新。
  - 法規20問を全問誤答→「復習（誤答20問中10問）」→復習は出題順の先頭10問(A-1〜A-10)ちょうど→全問正答→バンクは残り10問(未着手のB系列)に減少。
  - 英会話7問中1問(Q-2)誤答→復習で正答→バンク0問「すべて解消」→**復習ボタンが選択画面から消える**ことを確認。

### 仮定・設計判断(初回実装時点)
- `presented` を持たない旧形式の attempts(このセッション内のテストデータ等)は `currentMistakes` が安全側(判定不能として無視)に倒す。実ユーザーデータはまだ存在しないため影響なし。
- 復習回で「不正解」だった問題は、たとえ元の通常回でも誤答でも、直近の出題(=復習回)の結果で上書きされる。何回でも間違え続ける限りバンクに残り続ける設計。

### 追記(2026-07-22): 誤答バンクを科目ごと→全期間横断1本に統合
- 琥珀の要望: 「復習ボタンを科目ごとに置くのではなく、選択画面トップに『過去に間違えた問題集』として1つだけ置き、現在1期(R8-02)しかないデータが将来増えても自動的にその全期間から出題されるようにしたい」。
- **今すぐ実行可能と判断して即実装**(全期間展開を待つ必要なし): 問題idは元々 `{examCode}-{subject}-{no}` でグローバルに一意なので、誤答バンクの判定に科目・期のフィルタは元々不要だった。`data/index.json` の exam一覧を都度読みに行く設計にしたことで、期が増えても再実装なしで自動的に取り込まれる。
- **quiz-core.mjs**:
  - `currentMistakes(store, key)` → **`currentMistakes(store)`** に簡略化(key引数を削除、全attemptsを横断走査)。
  - 新規 `splitAttemptsByKey(questions, examResult, date, keyOf)`: 採点結果をキーごとに分割してAttempt配列を返す純関数。通常回(キー1種類)は退化して従来と同じ1件のAttemptになる。復習回が複数科目・複数期にまたがっても、`statsForKey`(科目別の挑戦回数/正答率)を汚さずに済む。
- **app.js**: 科目ごとの `.review-btn`/`.subject-row` を廃止。選択画面トップ(期カードのループより前)に `.global-review-btn`「過去に間違えた問題集（誤答N問）」を1つだけ表示。クリックで `startGlobalReview()` が `data/index.json` の全期を fetch・結合し、`pickReviewQuestions`(無改修で再利用)で最大10問を選ぶ(順序: 期の新しい順→ファイル内の元の順、決定的)。復習回は各問題に出典タグ(期・科目)を表示(科目・期が混在し得るため)。`grade()` は `splitAttemptsByKey` を通す1本の経路に統一。
- **TDD**: currentMistakes簡略化+splitAttemptsByKeyのテストを先に書き、新規タブで赤確認(importエラーで停止)→実装→緑(31/31)→意図的バグ(wrongSet判定を反転)で2件が赤に転じることを確認→復帰。
  - **キャッシュ事故への対策を確立**: このbrowserプレビュー環境はモジュールをURL単位でキャッシュすることがあり、新規タブ作成だけでは解消しないケースが今回発生した。確実な回避策は `fetch(url, {cache:'reload'})` で対象ファイルを個別に温めてから `location.reload()` すること(既存タブのままでも効く)。以後のJS/CSS変更の実機確認はこの手順を使う。
- **実機検証**(3シナリオ、ブラウザの実イベントで確認):
  - 単一期: 無線工学2問を誤答→選択画面トップに「過去に間違えた問題集（誤答2問）」→復習で出典タグ「令和8年2月期／無線工学」付きで出題→全問正解→「誤答バンク: 0問(すべて解消)」→ボタン消失。
  - 複数キー分割保存(2期目の実データが無いため無線工学/法規の異科目で代用): localStorageに2キー分の誤答を仕込む→復習で2問とも正しい出典タグ付きで出題→1問正答・1問誤答のまま採点→**localStorageのattempts件数が+2(1件でなく)**、既存2件はバイト同一のまま保持(append-only維持)、両科目の挑戦回数がそれぞれ独立して正しく増加(`statsForKey`は無汚染)。
  - 10問キャップ: 14件の誤答(工学8+法規6)を仕込み→ラベルが「誤答14問中10問」→出題が「工学A-1〜A-8→法規A-1,A-2」の決定的な順で先頭10件。
- 工学の図7枚(A1〜A5,B2,B3)を再クロップし、隣接設問のテキスト混入を除去(`scripts/crop_figures.py`で範囲を絞り、目視確認して1〜2回微調整、確立済みの手順どおり)。`data/R8-02.json`側のfigureパスは変更不要。

### 追記(2026-07-22): 全分野10問 + 科目ごと10問の内訳ボタンに拡張
- 琥珀の要望: 「全分野からの10問」に加えて「工学・法規・英語・英会話の4部門それぞれからの10問」も選べるようにしたい。
- **quiz-core.mjs は無改修**。`currentMistakes(store)`(全体の誤答id集合)と`pickReviewQuestions(questions, mistakeIds, max)`(idに一致する問題を出題順で最大max件)は元々subject非依存だったため、app.js側で「結合済みの全問題リストを科目でフィルタしてから`pickReviewQuestions`に渡す」だけで対応できた。
- **app.js**: `startGlobalReview()` → `startMistakeReview(subjectFilter)` にリネーム(`null`=全分野、科目名=その科目のみ)。選択画面初期化時に全期間・全科目の問題を1回結合して `state.allQuestionsCache` にキャッシュし、内訳カウント表示と復習開始の両方で使い回す(復習ごとの再フェッチを避ける)。`state.reviewSubject` を追加し、meta行・結果ヘッダー・誤答バンク残数表示がすべて「今どの範囲を見ているか」に応じて出し分けられるようにした(`remainingMistakeCount(subjectFilter)`)。
- 選択画面トップには、誤答が1件以上ある科目の分だけボタンが並ぶ(0件の科目は非表示)。並び順は 無線工学→法規→英語→英会話 の固定優先順(未知の科目が将来増えてもアルファベット順で末尾に追加される)。
- CSS: `.global-review-btn` → `.mistake-review-btn` にリネーム(全ボタン共通)。
- **実機検証**: 工学2問・法規1問を仕込み→「全分野・誤答3問」「無線工学・誤答2問」「法規・誤答1問」の3ボタンが出ることを確認→無線工学ボタンから復習(2問限定・meta「無線工学・復習」)→全問正解→「無線工学の誤答バンク: 0問(すべて解消)」→無線工学ボタンが消え、全分野は「誤答1問」に、法規ボタンは「誤答1問」のまま残ることを確認。
- 既存テスト31件は無改修のまま緑(quiz-core.mjsを触っていないため)。

### 次の初手
1. (琥珀判断待ち)全期間(2002〜2026)へ展開するか、GitHub Pages 公開を先にやるか。
2. 全期間展開のモデル選定: 大部分(法規・英語・英会話)はSonnet 5で十分(全問正答チェックによる自己検証が効くため)。工学のみ画像読解の精度リスクが高く、Opus切り替えか転記後の目視二重チェックを推奨(2026-07-22時点の助言、詳細はセッションログ参照)。

## Phase0(完了 — 2026-07-21)

### 完了したこと
- 素のHTML+JS(フレームワーク無し・ビルド不要)の静的アプリを実装。
  - `index.html` / `app.js`(表示・遷移・採点結線) / `style.css`(ダークモード対応・モバイル最適化) / `quiz-core.mjs`(採点と成績の純粋関数)。
- 実データ1期 `data/R8-02.json`(既存 `sample_R8-02.json` を移設)を読み込み。
- 画面:選択(年・期→科目、各科目に前回/最高正答率・挑戦回数)→ 出題(1問ずつ・前へ/次へ)→ 採点 → 誤答のみ一覧(問題文・自分の解答・正解・解説)。
- 成績を localStorage(`aviation_quiz_results`)に **append で保存**。
- TDD:採点・append-only・統計の純粋関数を先にテスト化。
  - node が無い環境のため、同じ `quiz-core.mjs` を **ブラウザのテストランナー**(`tests/run-tests.html`)で実行。
  - スタブ実装で **FAIL 16/16(赤)** を確認 → 本実装で **PASS 16/16(緑)**。
  - 意図的バグ(複合型 `every`→`some`)で **FAIL 4/16** に転じ、テストが回帰を捕捉することも確認。
- 実機検証(preview browser、デスクトップ+モバイル375px):
  - 無線工学(single×2)を解いて 1/2・50%、誤答一覧に A-1 が表示。
  - 法規(single + truefalse_list + fill_blanks)を全問正解で 3/3・100%。
  - localStorage に2件が **append-only** で残る(1件目が無傷)ことを実機で確認。
  - 選択画面の成績表示(前回50%/最高50%/挑戦1回 等)が反映。

### SPECからの確定した変更(実物サンプルを正とした)
- スキーマ:`choices`→`options`、`image`→`figure`。問題型を `single` / `truefalse_list` / `fill_blanks` の3型に拡張(法規B問題が4択単一で表現不能なため)。
- **`answer` は 1始まり index**(SPEC原文の0始まりから変更)。既存サンプルを再編集しないための決定。
- ファイル単位は**期ごと1ファイル**(全科目同居)。SPEC原文の科目別ファイルから変更。
- 上記はユーザー(琥珀)承認済み:「3型すべて」「実物サンプル準拠」。

### 仮定(要確認・後で見直す)
- `data/index.json` の `count`(問題数)は手書き。Phase1で data から自動生成して二重管理をやめる。
- `wrong` は 0始まり index。人間可読ではないので、結果画面表示は index→問題詳細を引いて出している。SPEC例の数値形と整合。
- 図は `figures/R8-02-kougaku-A1.png` にプレースホルダ(320x180グレー枠)を置いた。実際の回路図切り出しはPhase1のデータ整形で差し替え。
- 日付は `new Date().toISOString().slice(0,10)`(UTC基準)。端末TZによっては1日ずれ得る。実用上は許容。

### 未解決 / 既知の制限
- preview browser の `computer`(座標クリック/スクショ)がタイムアウトしやすい。検証は実イベント発火(input.click / change dispatch)で代替した=本物のリスナーは通っている。
- GitHub Pages への公開は**未実施**(下記手順を用意)。リモートリポジトリはユーザーが作成する必要がある。
- データは R8-02 の1期・サンプル7問のみ。実問題の全量投入はPhase1。

### 次の初手(Phase1)
1. `koukuu-tuu/` の生PDF1回分(できれば無線工学)から、`data/{code}.json` への整形手順を1本確立する(手作業+検算)。図の切り出しも1枚やってみて実測時間を測る。
2. `data/index.json` を data から生成するスクリプト(Python)を書く。
3. 「1年分の3科目」を作った時点で一旦止め、実測時間を見て全期間へ広げるか判断(SPECの撤退・凍結条件)。

## Phase1(進行中 — 2026-07-21〜)

### 確立したデータ変換パイプライン(半自動)
生PDFは数式が化け・表の読み順が乱れ・図が要るため、完全自動変換は不可。以下の半自動フローに確定:
1. `scripts/pdf_to_png.py <pdf> <out> [dpi]` … ページを PNG に描画(**画像を正とする**)。
2. 画像を目視で JSON へ転記(数式は読める文字へ: 例 Ė, X_L, π/2)。**正答は正答PDFを画像化して読む**(テキスト抽出は順序が乱れて不可)。
3. `scripts/crop_figures.py <pdf> <page> x0 y0 x1 y1 <out.png>` … 図をページ比率で切り出し。
4. `scripts/validate_data.py` … answer範囲・id重複・type・figure実在・options件数を機械検査。
5. `scripts/build_index.py` … data/*.json から `data/index.json` を自動生成(手書きの二重管理を廃止)。

依存: PyMuPDF(`pip install pymupdf`)。node/deno は無い環境。

### パイロット成果(無線工学 R8-02 = 最難関の科目)
- **全14問を JSON 化**(A-1〜A-10=single、B-1=truefalse_list、B-2〜B-4=fill_blanks)。`data/R8-02.json`。
- 図を7枚切り出し(A-1〜A-5, B-2, B-3)。アプリで全7枚が実寸描画されることを確認。
- 正答は正答PDF画像から取得しデータに反映。**アプリで全14問を正答で解き 14/14・100%**(=転記解答が正答表と一致)。
- `validate_data.py` を通過(19問・id一意・figure実在)。`build_index.py` で index.json 再生成(工学14/法規3/英会話2)。
- テストは緑のまま(run-tests.html: PASS 16/16)。

### 実測時間と観測(凍結判断の材料 — ここが Phase1 の主目的)
- 工程別コストの体感: **図の切り出し(clip-rect の当たり調整)と数式の文字起こしが支配的**。図7枚で各1〜2回の座標微調整が要った。
- テキストのみの科目(**法規・英語・英会話は図がほぼ無い**)は、転記のみで済み工学より大幅に速いと見込む。
- したがって「1年分の3科目」を仕上げるコストは、工学>>法規≒英語≒英会話。工学を1回やり切った今、残りは軽い。

### 判断(琥珀に確認したいこと)
- 凍結条件は「1年3科目で一旦止めて実測を見る」。R8-02 は工学を完了済み。**次に R8-02 の法規・英語(または英会話)を仕上げて『1年3科目』を満たすのが素直**。テキスト主体で速いはず。
- その後に全期間(2002〜2026)へ広げるかを、実測を見て判断する。**今ここでは広げない**(リサーチ沼防止)。

### 未解決 / 既知の制限
- 工学JSONは数式をプレーン文字(Ė, X_L, [kΩ] 等)で表現。**憲法どおり、学習に使う前に琥珀がPDFと突き合わせる目視パスを推奨**(特に選択肢の組合せ表)。
- 図の切り出しは広めに取ったため、一部に周辺の文字や設問番号(EZ802-1 等)が写り込む。実用上は可読だが、詰めるなら clip 座標の再調整。
- スクショツール(computer)がこのセッションで頻繁にタイムアウト。機能検証は実DOMイベント発火とネットワーク/画像ロード確認で代替した。

### 追記(2026-07-21): 英語(eigo)を投入
- R8-02 の英語(筆記 ERA802)を全12問JSON化(subject=英語)。英文解釈 A-1〜A-9=single、和文英訳 B-1〜B-3=fill_blanks。図なし。
- **schema拡張**: 任意の `passage` フィールドを追加(英文解釈の長文/規定文用)。app.js は passage をスクロール可能なボックスで設問の上に表示。validate は passage を任意扱い(チェック不要)。
- 問1(A-1〜5)は共通の長文をA-1に持たせ、A-2〜5は「(A-1参照)」。問2(A-6〜9)は各設問が個別の規定文を持つ。
- アプリで全12問を正答で解き 12/12・100%(正答表と一致)。validate 通過(31問・id一意)。build_index 再生成(工学14/法規3/英語12/英会話2)。
- 所要の体感: **図が無い分、工学より大幅に速い**(予想どおり)。支配的コストは長文の転記のみ。英文解釈の長文は1つを転記すれば問1の5問で使い回せる。
- これで R8-02 は工学(全)+英語(全)+法規/英会話(一部)。「1年3科目」の主要科目は工学・英語で充足。**全期間へはまだ広げない**(琥珀の指示)。

### 追記(2026-07-21): 法規を全問化 + 穴埋めインライン表示
- R8-02 の法規(EY802)を**全20問**JSON化(A-1〜A-14=single、B-1/B-4/B-5=truefalse_list、B-2/B-3/B-6=fill_blanks)。サンプル3問を差し替え。
- **これで R8-02 は工学14・法規20・英語12・英会話2 = 全48問。「1年分の主要3科目」を実データで充足**(英会話のみ一部)。
- 全問を正答で解いて 20/20・100% を実機確認。validate 通過(48問・id一意)。
- **穴埋め表示を改修**(琥珀の指摘対応): fill_blanks は本文の [ア]…[オ] マーカー位置に select をインライン描画し、未選択時は「(ア)」と表示。選択肢は番号付きプールで下に一覧。同じ記号が複数(例:B-2のウ)あるときは選択を同期。旧サンプルの不自然な「(ア=総則)」も正しい全文に修正。
- 全 fill_blanks の text を [ラベル] マーカーに統一(eigo の二重括弧 ([ア]) を [ア] に修正)。

### 実測(3科目ぶんの体感が揃った)
- 工学(図・数式)>> 法規(長い法令文だが図なし)≒ 英語(長文)。法規は問題数が多い(20)分の転記量はあるが、画像目視転記なので機械的。1科目あたりの所要は「重い工学」でも1セッション内で十分完了する規模と判明。
- **全期間(2002〜2026・各年3科目)へ広げるコスト見積もりの材料は十分揃った。ただし指示があるまで広げない**(琥珀の明示指示)。

### 追記(2026-07-21): 英会話を全問化 + 前回誤答インジケータ
- R8-02 の英会話(ERB802)を**全7問**JSON化(Q-1〜Q-7、各4択)。サンプル2問を差し替え。
- **R8-02 は工学14・法規20・英語12・英会話7 = 全53問。実データで4科目すべて完成**。
- **前回誤答インジケータ**を実装: 直近の挑戦で間違えた問題は、出題画面の問題番号横に「前回まちがえた」バッジを表示。
  - これに合わせ、成績の `wrong` を **0始まりindex → id 配列**に変更(id基準ならデータの問題順が変わっても突き合わせが崩れない)。quiz-core.gradeExam・結果画面の誤答一覧・ユニットテストを id 基準へ更新(tests 緑 16/16)。旧index形式の記録も読めるよう互換処理あり。
- アプリで英会話 7問・バッジ表示・53問validate通過を確認。

### 次の初手
1. (琥珀判断待ち)全期間(2002〜2026)へ展開するか、GitHub Pages 公開を先にやるか(iPad で使えるように)。
2. 全期間展開時は、pdf_to_png → 目視転記 → validate → build_index の流れをそのまま各年に適用。図の clip 座標はマニフェスト化を検討。

## GitHub Pages 公開手順(未実施・ユーザー操作が必要)

ローカルは `git init` 済み・初回コミット済み。以下はリモート作成と公開:

```
# 1. GitHubで空のリポジトリを作る(例: koukuu-quiz)。READMEは付けない。
# 2. このフォルダで:
git remote add origin https://github.com/<ユーザー名>/koukuu-quiz.git
git branch -M main
git push -u origin main
# 3. GitHub のリポジトリ → Settings → Pages →
#    Source: "Deploy from a branch" / Branch: main / フォルダ: /(root) → Save
# 4. 数分後 https://<ユーザー名>.github.io/koukuu-quiz/ で開ける(iPad/iPhoneで確認)。
```
注意:公開は外部への発信にあたるため、リモート追加・push はユーザーの明示操作で行う(このアプリはローカルcommitまで用意)。
